<project>
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.novell.lsg.gwtclient.example</groupId>
  <artifactId>server</artifactId>
  <name>Example Server :: GWTCLIENT packaging</name>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>erlang-otp</packaging>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <id>unpack</id>
            <phase>process-sources</phase>
            <goals>
              <goal>copy</goal>
            </goals>
            <configuration>
              <artifactItems>
                <artifactItem>
		  <groupId>com.novell.lsg.gwtclient.example</groupId>
		  <artifactId>client</artifactId>
		  <version>0.0.1-SNAPSHOT</version>
		  <type>gwtclient</type>
                  <outputDirectory>${project.build.directory}/priv</outputDirectory>
                </artifactItem>
              </artifactItems>
              <outputDirectory>${project.build.directory}/priv</outputDirectory>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>eu.lindenbaum</groupId>
        <artifactId>maven-erlang-plugin</artifactId>
        <version>1.0.0-beta</version>
        <extensions>true</extensions>
	<configuration>
	  <skipDialyzer>true</skipDialyzer>
	</configuration>
      </plugin>
    </plugins>
  </build>
</project> 
